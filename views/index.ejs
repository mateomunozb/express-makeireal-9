<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <table>
      <tr>
        <th>Id</th>
        <th>Name</th>
        <th>Visits</th>
      </tr>
      <%for(let i = 0; i < allVisitors.length; i++ ) { %>
      <tr>
        <td><%= allVisitors[i]._id %></td>
        <td><%= allVisitors[i].name %></td>
        <td><%= allVisitors[i].count %></td>
      </tr>
      <% } %>
    </table>
  </body>
</html>
